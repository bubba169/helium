id="{{ field.id }}"
name="{{ field.name }}"
class="{{ field.classList }}"
placeholder="{{ field.placeholder }}"

{% for attribute,value in field.attributes %}
    {% if value is same as(true) %}
        {{ attribute }}
    {% else %}
        {{ attribute }}="{{ value }}"
    {% endif %}
{% endfor %}
