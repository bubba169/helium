<div class="form-group">
    <input name="{{ field.fieldName }}" value="" type="hidden"/>
    {% if field.getAttribute('type') != hidden %}
        {% include "helium::input.common.label" %}
    {% endif %}
    <select {% include "helium::input.common.attributes" %} multiple>
        {% if (field.getAttribute('required') is empty) %}
            <option value="">{{ field.placeholder }}</option>
        {% endif %}

        {% for value,name in field.options %}
            <option
                value="{{ value }}"
                {{ value in field.value ? 'selected' }}
            >
                {{ name }}
            </option>
        {% endfor %}
    </select>
</div>
